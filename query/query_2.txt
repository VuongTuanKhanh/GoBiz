SELECT 
    img.image ImageUrl, 
    img.name ProductName, 
    img.location ShopLocation, 
    sold TotalSold, 
    img.url ProductLink, 
    cat.category_1, 
    cat.category_2, 
    seller.name Shop
FROM 
    `gobiz-solution.image_crawl.shopee` img
    left join `gobiz-solution.image_crawl.dim_shopee` cat on img.category= cat.url
    left join `gobiz-solution.image_crawl.shopee_seller` seller on img.shop_id= cast(seller.shopid as string)
where 
    cat.category_1="Thời Trang Nam"
limit 5000